/**
 * @file Index.tsx
 * @description Main page component that renders the Chat Scenario Builder layout
 * 
 * @dependencies BuilderLayout component
 * @usage Rendered at the "/" route in App.tsx
 */

import { BuilderLayout } from "@/components/builder/BuilderLayout";

const Index = () => {
  return <BuilderLayout />;
};

export default Index;
