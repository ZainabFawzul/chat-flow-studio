/**
 * @file example.test.ts
 * @description Example test file demonstrating Vitest usage with describe/it/expect
 * 
 * @dependencies vitest
 * @usage Template for writing unit tests
 */

import { describe, it, expect } from "vitest";

describe("example", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});
