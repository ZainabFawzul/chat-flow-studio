/**
 * @file use-toast.ts
 * @description Re-export of toast hook for convenient imports from UI components folder
 * 
 * @see src/hooks/use-toast.ts
 */

import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
