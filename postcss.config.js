/**
 * @file postcss.config.js
 * @description PostCSS configuration for Tailwind CSS processing and autoprefixer
 * 
 * @dependencies tailwindcss, autoprefixer
 * @usage Used by PostCSS during build to process CSS files
 */

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
